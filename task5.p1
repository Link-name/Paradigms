% Правило для вычисления суммы пустого списка
sum_list([], 0).

% Правило для вычисления суммы списка, где Head - первый элемент, Tail - хвост списка
sum_list([Head | Tail], Sum) :-
    sum_list(Tail, TailSum),   % Рекурсивный вызов для хвоста списка
    Sum is Head + TailSum.     % Сумма равна сумме хвоста плюс первый элемент

% Пример использования
% sum_list([1, 2, 3, 4, 5], X). % Вызов для списка [1, 2, 3, 4, 5]
